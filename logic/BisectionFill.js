export default function BisectionFill(i,L,R,root) {
  const i_Arr =[]
  const L_arr =[]
  const R_arr =[]
  const root_arr= []
  i_Arr.push(i);
  L_arr.push(L);
  R_arr.push(R);
  root_arr.push(root);
};